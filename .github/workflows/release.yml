# # This file is generated by wujibear-studio/autonomoushive-openapispec
# name: Relase
# on:
#   push:
#     branches:
#       - main

# jobs:
#   lint-code:
#     runs-on: ubuntu-latest

#     steps:
#     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
#     - uses: actions/checkout@v3
#       name: Checkout code

#     - name: Set Version
#       run: echo "VERSION=$(npx yaml-cli get AutonomousHiveAPI.yaml info.version)" >> $GITHUB_ENV

#     # TODO: add GH tags and NPM releases
#     # See: https://github.com/marketplace/actions/github-tag
